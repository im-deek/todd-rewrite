{
  "help": [
    {
      "command": "ping",
      "description": "Returns pong and the latency to check if the bot is up."
    },
    {
      "command": "prefix {newPrefix}",
      "description": "Changes the prefix in this server to the value of `{newPrefix}`. If left blank it displays the current prefix. The prefix can also be checked by tagging the bot in case you forget it."
    },
    {
      "command": "pp {user}",
      "description": "I don't think I need to explain this one. `{user}` must be a mention or a username with the discriminator."
    }
  ]
}